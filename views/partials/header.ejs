<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
    <h3 class="text-xl">Scatch</h3>
    <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
    <% if (loggedin) { %>
        <div class="flex gap-5">
            <a href="/shop" class="transition-all duration-150 hover:font-bold hover:text-red-500 hover:underline hover:scale-105">shop</a>
            <a href="/cart" class="transition-all duration-150 hover:font-bold hover:text-red-500 hover:underline hover:scale-105">Cart</a>
            <a href="/account" class="transition-all duration-150 hover:font-bold hover:text-red-500 hover:underline hover:scale-105">My Account</a>
            <a class="text-red-600 transition-all duration-150 hover:font-bold hover:underline hover:scale-105" href="/users/logout">Logout</a>
        </div>
    <% } %>
</nav>
