<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-gray-50">
    <% var loggedin = typeof user !== "undefined" && user !== null %>
    

    <% if (loggedin) { %>
    <nav class="w-full flex justify-between items-center px-6 py-4 bg-white shadow">
        <!-- Brand name (hidden when not logged in) -->
        <a href="/" class="text-2xl font-bold text-blue-600">Scatch</a>

        <!-- Navbar links -->
        <div class="flex gap-6 items-center text-gray-700 font-medium">
            <a href="/shop" class="hover:text-blue-600 hover:underline">Shop</a>
            <a href="/cart" class="hover:text-blue-600 hover:underline">Cart</a>
            <a href="/account" class="hover:text-blue-600 hover:underline">My Account</a>
            <a href="/account/orders" class="hover:text-blue-600 hover:underline">My Orders</a>
            <a href="/logout" class="text-red-500 hover:underline">Logout</a>
        </div>
    </nav>
    <% } %>
